<%- include('partials/header') %>

<div class="centered-content">
  <% if (user) { %>
    <h2>Bem-vindo, <%= user.name %>!</h2>
    <p>Você está autenticado como <strong><%= user.role %></strong>.</p>
    <p>Escolha uma opção no menu para começar.</p>
  <% } else { %>
    <h2>Bem-vindo ao Sistema de Formação Contínua</h2>
    <p>Por favor, faça login ou registre-se para acessar as funcionalidades.</p>
  <% } %>
</div>

<%- include('partials/footer') %>
